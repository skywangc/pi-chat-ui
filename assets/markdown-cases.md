# 一级标题与中文排版

正文含 **粗体**、*强调*、~~删除线~~、`inline_code()` 和中文标点；~/.config 是路径。价格 $20 与 $30 是金额。

## 二级标题

1. 第一项
   - 内层条目
   - [x] 已完成
   - [ ] 待处理
2. 第二项

> 引用第一段。
>
> 引用第二段，含 `path/to/file.ts`。

| 状态 | 内容 |
| --- | --- |
| 运行中 | 中文和 English mixed |
| 完成 | 测试表格溢出 abcdefghijklmnopqrstuvwxyz0123456789 |

```typescript
const message = "你好，世界";
const longLine = "abcdefghijklmnopqrstuvwxyz0123456789abcdefghijklmnopqrstuvwxyz0123456789abcdefghijklmnopqrstuvwxyz0123456789";
```

公式：$x^2 + y^2 = z^2$。

$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$

```mermaid
flowchart LR
  A[输入] --> B[模型]
  B --> C[工具]
```

[外部网页](https://example.com) · [工作区文件](./src/App.tsx) · [不安全链接](javascript:alert%281%29)

![不可用图片替代文本](./missing-test-image.png)

下面故意保留未闭合围栏，对比 streaming 容错与 static 原文处理。

```text
unfinished fence
